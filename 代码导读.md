# 代码导读（基于当前分支最新改造）

这份文档给后续维护者，目标是：

1. 快速看懂项目主链路
2. 看懂我们相对 upstream 的内部改造，尤其是 **登录链路隔离**

---

## 1. 一句话架构

这是一个 Rust workspace：

- **CLI**（`codex-rs/cli`）负责入口与命令路由
- **TUI**（`codex-rs/tui`）负责交互界面
- **Core / Provider / Login** 负责会话、模型请求、认证

可以理解为：**一个核心引擎 + 多个入口前端**。

---

## 2. 这次最重要的改造点（必读）

### 2.1 登录冲突隔离：把内部 SSO 从 `cli/src/login.rs` 拆出

为减少每次 merge upstream 时在 `login.rs` 的高频冲突，当前分支采用：

- `codex-rs/cli/src/login.rs`：尽量保持接近 upstream（高冲突文件）
- `codex-rs/cli/src/login_sso_internal.rs`：承载内部 SSO 登录/状态/登出逻辑
- `codex-rs/cli/src/lib.rs`：对外导出内部 SSO 入口

当前导出关系：

- `run_sso_login`
- `run_login_status`
- `run_logout`

都从 `login_sso_internal` 导出，而不是从 `login` 导出。

### 2.2 CLI 入口行为

`codex-rs/cli/src/main.rs` 里：

- 登录默认路径仍走内部 SSO（`run_sso_login(...)`）
- 保留 `--env`（`prod/sit`）用于运行时 SSO 环境

### 2.3 内部认证主链路

- `codex-rs/login/src/sso_config.rs`
- `codex-rs/login/src/sso_login.rs`
- `codex-rs/model-provider/src/cookie_auth_provider.rs`

语义：**SSO + cookie** 是内部主认证方式；provider 仍然有效（路由、endpoint、catalog 等）。

---

## 3. 推荐阅读顺序（最短路径）

### 第一步：入口

- `codex-rs/cli/src/main.rs`
- `codex-rs/cli/src/lib.rs`
- `codex-rs/cli/src/login_sso_internal.rs`

先确认：登录命令最终调用哪个函数、`--env` 如何传递。

### 第二步：认证实现

- `codex-rs/login/src/sso_config.rs`
- `codex-rs/login/src/sso_login.rs`
- `codex-rs/login/src/auth/manager.rs`

先确认：SSO session 如何创建、落盘、读取。

### 第三步：请求如何带认证

- `codex-rs/model-provider/src/auth.rs`
- `codex-rs/model-provider/src/cookie_auth_provider.rs`
- `codex-rs/model-provider/src/provider.rs`
- `codex-rs/model-provider/src/models_endpoint.rs`

先确认：请求用 bearer 还是 cookie、base_url 从哪里来。

### 第四步：UI 行为

- `codex-rs/tui/src/lib.rs`
- `codex-rs/tui/src/app.rs`
- `codex-rs/tui/src/chatwidget.rs`

先确认：无登录态时 UI 如何处理，状态线与弹窗是否符合内部预期。

---

## 4. 相对 upstream 的模块差异（当前快照）

### CLI

- `M codex-rs/cli/src/lib.rs`
- `A codex-rs/cli/src/login_sso_internal.rs`
- `M codex-rs/cli/src/main.rs`

### Login

- `M codex-rs/login/src/auth/manager.rs`
- `M codex-rs/login/src/lib.rs`
- `A codex-rs/login/src/sso_config.rs`
- `A codex-rs/login/src/sso_login.rs`

### Model Provider

- `M codex-rs/model-provider-info/src/lib.rs`
- `M codex-rs/model-provider/src/auth.rs`
- `A codex-rs/model-provider/src/cookie_auth_provider.rs`
- `M codex-rs/model-provider/src/lib.rs`
- `M codex-rs/model-provider/src/models_endpoint.rs`
- `M codex-rs/model-provider/src/provider.rs`

### TUI

核心改动集中在 app/chatwidget/status/snapshots（与登录态展示联动）。

### Scripts / Distribution

- `A xhs.sh`
- `A install-codex-cli.sh`
- `A model_catalog.json`

---

## 5. 维护建议（减少后续冲突）

1. **内部逻辑优先加在 `login_sso_internal.rs`**，避免直接改 `login.rs`。
2. merge upstream 时，先看 `cli/src/lib.rs` 导出是否还对齐。
3. 每次登录链路改动后至少做：
   - `just fmt`（在 `codex-rs/`）
   - `cargo test -p codex-cli`
4. 若 `login.rs` 再次冲突，先以“保持接近 upstream + 内部逻辑放独立模块”为原则处理。

---

## 6. 相关文档

- `AGENTS.md`：内部改造总览、GUI 启动现实、风险点、回滚点（已并入）
- `xhs.sh` / `install-codex-cli.sh`：内部发布与安装路径


---

## 附录：新同学 30 分钟 Onboarding 阅读清单

> 目标：30 分钟内建立“能改能定位问题”的最小心智模型。

### 0-5 分钟：先看仓库定位

1. `README.md`（根目录）
2. `AGENTS.md`（Internal Fork Context 章节）

你要先知道：这是 upstream fork，不是完全跟随 upstream 的纯镜像。

### 5-12 分钟：看 CLI 登录入口（最关键）

1. `codex-rs/cli/src/main.rs`（只看 Login/Logout 路由）
2. `codex-rs/cli/src/lib.rs`（看导出指向）
3. `codex-rs/cli/src/login_sso_internal.rs`（内部 SSO 真正逻辑）

你要回答：

- 登录默认走哪条链路？
- `--env` 在哪里生效？
- 为什么要把内部逻辑放 `login_sso_internal.rs`？

### 12-20 分钟：看认证与 provider 主链路

1. `codex-rs/login/src/sso_config.rs`
2. `codex-rs/login/src/sso_login.rs`
3. `codex-rs/model-provider/src/cookie_auth_provider.rs`
4. `codex-rs/model-provider/src/auth.rs`

你要回答：

- SSO session 如何拿到并落盘？
- 请求最终如何带 cookie / auth？

### 20-26 分钟：看 UI 如何消费登录态

1. `codex-rs/tui/src/lib.rs`
2. `codex-rs/tui/src/app.rs`
3. `codex-rs/tui/src/chatwidget.rs`

你要回答：

- 无登录态时用户看到什么？
- 登录相关状态在 UI 哪里展示？

### 26-30 分钟：看发布与安装链路

1. `xhs.sh`
2. `install-codex-cli.sh`
3. `model_catalog.json`

你要回答：

- 为什么内部发布名是 `codex-cli`？
- 安装脚本写入到哪里？（`~/.codex`）

### Day-1 实操建议（可选）

- 先跑一条最小验证：
  - `cd codex-rs && just fmt`
  - `cd codex-rs && cargo test -p codex-cli`
- 先做一类“低风险改动”：仅改 `login_sso_internal.rs` 的日志/提示文案并验证。

